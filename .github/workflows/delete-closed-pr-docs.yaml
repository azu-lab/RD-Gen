name: delete-closed-pr-docs

on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch:

jobs:
  delete-closed-pr-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Delete closed PR docs
        uses: ./.github/workflows/delete-closed-pr-docs-sub@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
